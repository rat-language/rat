void fizzbuzz(n:int){
  for i in 0...n {
  	str fbnum = "";
    if ((i % 3==0) || (i % 5 == 0)){
      if (i % 3 == 0){ fbnum += "Fizz"; }
      if (i % 5==0){ fbnum += "Buzz"; }
  	  print(fbnum);
    }else{
      print(i);
    }
 }
}

// prints fizzbuzz up to 16 in this case
fizzbuzz(16);